# Brickpilot Web UI

The user-facing Brickpilot web UI now lives in the sibling repo:

`/Users/brick/comma-dev/brickpilot-web-ui`

This `sunnypilot-src` repo keeps Brickpilot/openpilot code, Python workers, ML/log
tooling, and reusable drive-test scripts. Keep web UI app changes in the sibling
repo so Brickpilot vehicle-code commits are not required for UI-only updates.
